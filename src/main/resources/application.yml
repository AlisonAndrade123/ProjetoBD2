# Código ATUALIZADO para o arquivo: application.yml (sem a seção liquibase)
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/mini_mundo_db
    username: minimundo_user
    password: minimundo_pwd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  data:
    mongodb:
      uri: mongodb://localhost:27017/mini_mundo_db
    redis:
      host: localhost
      port: 6379

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

minio:
  url: http://localhost:9000
  accessKey: minio_admin
  secretKey: minio_password
  bucketName: produtos